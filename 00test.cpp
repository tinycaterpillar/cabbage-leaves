#include <bits/stdc++.h>
#define endl '\n' // don't use when you cover interactive problem

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int s; cin >> s;
    cout << s << endl;

    return 0;
}